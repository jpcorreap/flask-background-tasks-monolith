setup:
  addons:
    - plan: heroku-redis
      as: redis
    - plan: memcachier
      as: memcache
build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web: gunicorn wsgi:app
  worker: poetry run celery -A tasks worker -l info -E
